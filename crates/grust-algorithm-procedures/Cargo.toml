[package]
name = "grust-algorithm-procedures"
version = "0.17.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Registry adapters for Grust's reusable property-graph algorithms."
keywords.workspace = true
categories.workspace = true

[dependencies]
grust-algorithms = { version = "0.17.0", path = "../grust-algorithms" }
grust-procedures = { version = "0.17.0", path = "../grust-procedures" }
grust-core = { version = "0.17.0", path = "../grust-core" }
serde_json.workspace = true

[dev-dependencies]
grust-cypher = { version = "0.17.0", path = "../grust-cypher" }

[lints.rust]
unsafe_code = "forbid"
