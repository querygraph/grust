[package]
name = "grust-turso"
version = "0.19.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Turso GraphStore backend for Grust."
keywords.workspace = true
categories.workspace = true

[features]
default = []
sync = ["turso/sync"]
# Turso's own default allocator, as the global allocator of the whole binary.
mimalloc = ["turso/mimalloc"]

[dependencies]
async-trait = { workspace = true }
chrono = "0.4"
grust-core = { version = "0.19.0", path = "../grust-core" }
grust-sql-core = { version = "0.19.0", path = "../grust-sql-core" }
serde_json = { workspace = true }
tokio = { workspace = true }
turso = { workspace = true, default-features = false }
grust-cypher = { version = "0.19.0", path = "../grust-cypher" }

[dev-dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
tempfile = { workspace = true }
tokio = { workspace = true }
