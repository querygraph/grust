[package]
name = "grust-postgres-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Shared PostgreSQL GraphStore backend implementation for Grust."
keywords.workspace = true
categories.workspace = true

[dependencies]
grust-cypher = { version = "0.13.0", path = "../grust-cypher" }
async-trait = { workspace = true }
futures-util = { version = "0.3.32", default-features = false, features = ["std"] }
grust-core = { version = "0.13.0", path = "../grust-core" }
grust-sql-core = { version = "0.13.0", path = "../grust-sql-core" }
serde_json = { workspace = true }
tokio = { workspace = true }
tokio-postgres = { workspace = true }
