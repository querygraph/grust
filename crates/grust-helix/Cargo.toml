[package]
name = "grust-helix"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "HelixDB GraphStore backend for Grust."
publish = false  # Internal backend: independently validated and intentionally outside the facade.
keywords.workspace = true
categories.workspace = true

[dependencies]
async-trait = { workspace = true }
grust-core = { version = "0.21.0", path = "../grust-core" }
helix-db = { workspace = true }
reqwest = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
