# Portable result admission qualification

Native Capitola tests and warnings-denied all-target Clippy passed for decoder
source `579c7ab` and integrated controlled collection `ff5e895`. Raw logs retain
all outcomes. Commands: `cargo test --locked -p grust-datafusion --all-features`
and `cargo clippy --locked -p grust-datafusion --all-features --all-targets --
-D warnings`, four nice Cargo jobs. This qualifies logical output-copy admission,
not full operator accounting, automatic routing or a performance claim.
